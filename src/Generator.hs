module Generator where

import           System.Random


-- pluggable for different data types
-- configurable to change the event occurances

-- a generic config where you can change the occurances of each field in the datatypes

-- can just leave this out and come back later when know more about generics when merging event streams with different data types
